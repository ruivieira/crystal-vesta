image: ubuntu/latest
packages:
  - curl
  - unzip
sources:
  - https://git.sr.ht/~ruivieira/crystal-template
tasks:
  - setup: |
      curl -fsSL https://crystal-lang.org/install.sh | sudo bash
  - test: |
      cd crystal-template
      shards install
      crystal spec
      crystal spec
